#### Project Building GuideBook ####

build_test_script:
    description: 
        "This is a base script to get the minimal viable product before considering external factors"
    factors of the Project:
        - "Structure or Design"
        - "Performance or Memory"
        - "Usage or Purpose"
    notes:
        "DON'T MOVE TO THE NEXT STEPS UNTIL WHAT ABOVE IS COMPLETE!!!"

todos:
    implement_cli:
        - create_simple_cli_tool
            - Config cli interface
            - Execute <build_test_script> functionalities (MVP)
        - build_on_simple_cli_tool
            - Implement subcommands
            - Organize and group commands by class
